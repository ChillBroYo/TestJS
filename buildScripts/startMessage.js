import chalk from 'chalk';
//var chalk = require('chalk') standard es 5 (ECMAScript which is a standardization of JS)
//Babel approach --> the reason that babel is added to node in the package.json

console.log(chalk.green("Starting app in dev mode...")); //eslint-disable-line no-console
